async function run() { const { Jimp } = require('jimp'); const image = await Jimp.read('C:/Users/aryn1/Documents/Portfolio/PortfolioIDE/assets/images/projects/aibot.png'); image.scan(0, 0, image.bitmap.width, image.bitmap.height, function(x, y, idx) { let r = this.bitmap.data[idx]; let g = this.bitmap.data[idx+1]; let b = this.bitmap.data[idx+2]; let dist = Math.sqrt(Math.pow(255-r,2)+Math.pow(255-g,2)+Math.pow(255-b,2)); if (dist < 180) { this.bitmap.data[idx+3] = Math.floor(Math.max(0, (dist / 180) * 255)); } }); await image.write('C:/Users/aryn1/Documents/Portfolio/PortfolioIDE/assets/images/projects/aibot.png'); console.log('Fixed'); } run().catch(console.error);
